type Stock {
  id: ID! @goTag(key: "sql", value: "primary_key")
  productId: ID!
  product: Product
  storeId: ID!
  store: Store
  branchId: ID!
  branch: Branch
  createdAt: Time!
  updatedAt: Time!
  createdById: ID
  createdBy: CreatedByUser
  updatedById: ID
  updatedBy: UpdatedByUser
}

input CreateStock {
  productId: ID!
  storeId: ID!
  branchId: ID!
}
